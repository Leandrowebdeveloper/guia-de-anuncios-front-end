import { AdBlockedPipe } from './ad-blocked.pipe';

describe('AdBlockedPipe', () => {
  it('create an instance', () => {
    const pipe = new AdBlockedPipe();
    expect(pipe).toBeTruthy();
  });
});
