import { IsPriceCategoryAnnouncementPipe } from './is-price-category-announcement.pipe';

describe('IsPriceCategoryAnnouncementPipe', () => {
  it('create an instance', () => {
    const pipe = new IsPriceCategoryAnnouncementPipe();
    expect(pipe).toBeTruthy();
  });
});
