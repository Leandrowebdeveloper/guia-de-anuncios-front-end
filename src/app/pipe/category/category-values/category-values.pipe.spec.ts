import { categoryValuesPipe } from './category-values.pipe';

describe('categoryValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new categoryValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
