import { AnnouncementCategoryToggleIconPipe } from './toggle-icon-header.pipe';

describe('AnnouncementCategoryToggleIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementCategoryToggleIconPipe();
    expect(pipe).toBeTruthy();
  });
});
