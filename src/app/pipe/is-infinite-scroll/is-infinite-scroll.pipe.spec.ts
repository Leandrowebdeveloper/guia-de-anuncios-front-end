import { IsInfiniteScroll } from './is-infinite-scroll.pipe';

describe('IsInfiniteScroll', () => {
  it('create an instance', () => {
    const pipe = new IsInfiniteScroll();
    expect(pipe).toBeTruthy();
  });
});
