import { UserValuesPlanPipe } from './user-values-plan.pipe';

describe('UserValuesPlanPipe', () => {
  it('create an instance', () => {
    const pipe = new UserValuesPlanPipe();
    expect(pipe).toBeTruthy();
  });
});
