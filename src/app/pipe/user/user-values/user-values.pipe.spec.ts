import { UserValuesPipe } from './user-values.pipe';

describe('UserValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
