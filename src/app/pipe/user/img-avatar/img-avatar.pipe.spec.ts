import { ImgAvatarPipe } from './img-avatar.pipe';

describe('ImgAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
