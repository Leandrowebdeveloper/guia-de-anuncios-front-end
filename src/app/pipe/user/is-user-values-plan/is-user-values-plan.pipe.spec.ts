import { IsUserValuesPlanPipe } from './is-user-values-plan.pipe';

describe('IsUserValuesPlanPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUserValuesPlanPipe();
    expect(pipe).toBeTruthy();
  });
});
