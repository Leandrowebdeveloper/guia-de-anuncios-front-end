import { IsSocialNetworksPipe } from './is-social-networks.pipe';

describe('IsSocialNetworksPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSocialNetworksPipe();
    expect(pipe).toBeTruthy();
  });
});
