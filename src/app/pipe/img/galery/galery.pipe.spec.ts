import { TypeImageGaleryPipe } from './galery.pipe';

describe('TypeImageGaleryPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeImageGaleryPipe();
    expect(pipe).toBeTruthy();
  });
});
