import { IsAnnouncementPipe } from './is-announcement.pipe';

describe('IsAnnouncementPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAnnouncementPipe();
    expect(pipe).toBeTruthy();
  });
});
