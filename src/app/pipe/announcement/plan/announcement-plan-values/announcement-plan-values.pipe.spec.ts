import { AnnouncementPlanValuesPipe } from './announcement-plan-values.pipe';

describe('AnnouncementPlanValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementPlanValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
