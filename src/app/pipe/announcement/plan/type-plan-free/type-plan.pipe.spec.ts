import { TypePlanFreePipe } from './type-plan.pipe';

describe('TypePlanFreePipe', () => {
  it('create an instance', () => {
    const pipe = new TypePlanFreePipe();
    expect(pipe).toBeTruthy();
  });
});
