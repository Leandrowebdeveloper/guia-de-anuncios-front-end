import { IsWorkDaysValuesPipe } from './is-work-days-values.pipe';

describe('IsWorkDaysValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new IsWorkDaysValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
