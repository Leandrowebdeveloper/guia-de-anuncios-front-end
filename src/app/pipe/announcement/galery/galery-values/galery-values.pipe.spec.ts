import { GaleryValuesPipe } from './galery-values.pipe';

describe('GaleryValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new GaleryValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
