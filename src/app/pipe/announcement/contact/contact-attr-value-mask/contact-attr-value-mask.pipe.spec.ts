import { ContactAttrValuesMaskPipe } from './contact-attr-value-mask.pipe';

describe('ContactAttrValuesMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactAttrValuesMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
