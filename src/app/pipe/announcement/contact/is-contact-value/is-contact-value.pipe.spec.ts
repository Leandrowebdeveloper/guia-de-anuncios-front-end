import { AnnouncementContactValuesPipe } from './is-contact-value.pipe';

describe('AnnouncementContactValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementContactValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
