import { IsAttrContactPipe } from './is-attr-contact.pipe';

describe('IsAttrContactPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAttrContactPipe();
    expect(pipe).toBeTruthy();
  });
});
