import { IsAnnouncementAttrValuesPipe } from './is-announcement-attr-values.pipe';

describe('IsAnnouncementAttrValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAnnouncementAttrValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
