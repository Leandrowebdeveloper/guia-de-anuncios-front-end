import { AnnouncementValuesPipe } from './is-announcement-values.pipe';

describe('AnnouncementValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
