import { IsAnnouncementValuePricePipe } from './is-announcement-values-price.pipe';

describe('IsAnnouncementValuePricePipe', () => {
  it('create an instance', () => {
    const pipe = new IsAnnouncementValuePricePipe();
    expect(pipe).toBeTruthy();
  });
});
