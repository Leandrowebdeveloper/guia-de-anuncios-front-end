import { IsAttrAnnouncementUserPipe } from './is-attr-user.pipe';

describe('IsAttrAnnouncementUserPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAttrAnnouncementUserPipe();
    expect(pipe).toBeTruthy();
  });
});
