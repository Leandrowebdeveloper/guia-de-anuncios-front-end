import { AnnouncementUserValuesPipe } from './user-attr-values.pipe';

describe('AnnouncementUserValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementUserValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
