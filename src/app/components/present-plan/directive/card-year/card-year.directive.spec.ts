import { CardYearDirective } from './card-year.directive';

describe('CardYearDirective', () => {
  it('should create an instance', () => {
    const directive = new CardYearDirective();
    expect(directive).toBeTruthy();
  });
});
