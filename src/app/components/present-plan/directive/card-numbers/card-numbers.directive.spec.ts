import { CardNumbersDirective } from './card-numbers.directive';

describe('CardNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
