import { CardMonthDirective } from './card-month.directive';

describe('CardMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMonthDirective();
    expect(directive).toBeTruthy();
  });
});
